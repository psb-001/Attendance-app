# INTEGRATIONS

## Supabase
- Providing Auth (Students / Teachers)
- Storing `profiles` and `attendance_logs` inside a PostgreSQL DB.

## Local Files / Legacy
- Formerly tracking `attendance.json` and `students.json` inside `/data/` directory.

## Devices
- Integrates with device media storage (`expo-media-library`) for exporting records.
